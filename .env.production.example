# ============================================
# DearMe 프로덕션 환경변수 설정 가이드
# ============================================
# 이 파일을 복사하여 .env.production 으로 저장 후 값을 입력하세요
# cp .env.production.example .env.production
# ============================================

# --------------------------------------------
# 1. 데이터베이스 설정
# --------------------------------------------
# PostgreSQL 접속 정보 (프로덕션용 강력한 비밀번호 사용)
DB_USER=dearme
DB_PASSWORD=여기에_강력한_비밀번호_입력    # 예: Xk9$mP2@nQ5!wL8#
DB_NAME=dearme

# --------------------------------------------
# 2. 보안 설정 (중요!)
# --------------------------------------------
# JWT 토큰 서명용 비밀키
# 생성 방법: 터미널에서 아래 명령어 실행
#   openssl rand -hex 32
# 또는
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=여기에_생성한_64자_16진수_문자열_입력

# --------------------------------------------
# 3. OpenAI API 설정
# --------------------------------------------
# OpenAI 대시보드에서 API 키 발급: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-여기에_OpenAI_API_키_입력

# --------------------------------------------
# 4. Cloudflare Tunnel 설정 (HTTPS용)
# --------------------------------------------
# 설정 방법:
# 1. Cloudflare 대시보드 접속: https://one.dash.cloudflare.com
# 2. Zero Trust > Networks > Tunnels 메뉴 클릭
# 3. "Create a tunnel" 클릭
# 4. 터널 이름 입력 (예: dearme-prod)
# 5. "Docker" 선택 후 표시되는 토큰 복사
# 6. 아래에 토큰 붙여넣기
CLOUDFLARE_TUNNEL_TOKEN=여기에_Cloudflare_터널_토큰_입력

# --------------------------------------------
# 5. 도메인 설정
# --------------------------------------------
# Cloudflare Tunnel에서 설정한 도메인
# 예: api.dearme.com, dearme.yourdomain.com
API_DOMAIN=https://api.yourdomain.com
FRONTEND_DOMAIN=https://yourdomain.com

# --------------------------------------------
# 6. 디버그 모드 (프로덕션에서는 반드시 false)
# --------------------------------------------
DEBUG=false
